---
/** Style */
import "./loading.scss";

export interface Props {
    IDName?: string;
    className?: any;
    attribute?: attribute;
    style?: { [key: string]: string };
}

const { IDName, className, style, attribute = {} } = Astro.props as Props;
---

<div id={IDName} class:list={[className, "section-loading"]} style={style} {...attribute}>
    <div class="loader"></div>
</div>